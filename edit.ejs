<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Post</title>
</head>
<body> 
    <h2>Edit Your Post</h2>
    <p>Post id:<%= article.id %></p>
    <p>Post Username: @<%= article.username %></p>
    <p>Post contact: <%= article.contact %></p>
    <form method="post" action="/articles/<%= article.id %>?_method=PATCH">
        <textarea row="10" cols="35" name="content">  <%= article.content %></textarea>
        <button>Submit</button>
    </form>
    
</body>
</html>